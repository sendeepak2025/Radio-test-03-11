# 🔄 Network Flow Diagram - Before & After Fix

## ❌ BEFORE FIX - Network Error Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER BROWSER                            │
│  http://localhost:3010/reporting?studyUID=test-123              │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ 1. Page loads
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    FRONTEND (React/Vite)                        │
│  - ReportingPage.tsx                                            │
│  - StructuredReportingUnified.tsx                               │
│  - UnifiedReportEditor.tsx                                      │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ 2. Calls reportsApi.listByStudy()
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    ReportsApi.ts                                │
│  baseURL: http://localhost:8001  ❌ WRONG!                      │
│  path: /api/reports/study/test-123                              │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ 3. Direct request (bypasses proxy)
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    CORS ERROR                                   │
│  ❌ Access blocked by CORS policy                               │
│  ❌ No 'Access-Control-Allow-Origin' header                     │
└─────────────────────────────────────────────────────────────────┘
                             │
                             │ 4. Generic error thrown
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    USER SEES                                    │
│  ❌ "Network Error"                                             │
│  ❌ No details                                                  │
│  ❌ No way to debug                                             │
│  ❌ Complete failure                                            │
└─────────────────────────────────────────────────────────────────┘
```

---

## ✅ AFTER FIX - Successful Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER BROWSER                            │
│  http://localhost:3010/reporting?studyUID=test-123              │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ 1. Page loads
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    FRONTEND (React/Vite)                        │
│  📋 ReportingPage initialized                                   │
│  🔄 Workflow: selection → template                              │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ 2. Test connectivity first
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    ReportsApi.ping()                            │
│  🔍 Testing backend connectivity...                             │
│  baseURL: /api (uses Vite proxy) ✅ CORRECT!                    │
│  path: /api/reports/health                                      │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ 3. Request via Vite proxy
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    VITE DEV SERVER                              │
│  🔄 Proxying: GET /api/reports/health                           │
│     → http://localhost:8001/api/reports/health                  │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ 4. Forwarded to backend
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    BACKEND (Express)                            │
│  [REPORTS API] GET /api/reports/health                          │
│  ✅ Health endpoint (no auth required)                          │
│  Returns: { ok: true, service: 'unified-reporting', ... }       │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ 5. Response back through proxy
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    VITE DEV SERVER                              │
│  ✅ Response: 200 /api/reports/health                           │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ 6. Response to frontend
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    ReportsApi.ts                                │
│  ✅ Backend health check passed                                 │
│  Console: Full URL logged for debugging                         │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ 7. Continue with report loading
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    useReportState.ts                            │
│  📋 Looking for existing drafts...                              │
│  Calls: reportsApi.listByStudy(studyUID)                        │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ 8. Same proxy flow
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  VITE PROXY → BACKEND → VITE PROXY → FRONTEND                  │
│  ✅ All requests logged                                         │
│  ✅ Clear error messages if failure                             │
│  ✅ Graceful fallback if backend down                           │
└─────────────────────────────┬────────────────────────────────────┘
                             │
                             │ 9. Success or graceful failure
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    USER SEES                                    │
│  ✅ Report editor loads                                         │
│  OR                                                             │
│  🔴 "API DISCONNECTED - LOCAL MODE" banner                      │
│     with "Test Connection" button                               │
│  ✅ Clear, actionable error messages                            │
│  ✅ Console logs for debugging                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔍 Key Differences

### URL Resolution

**BEFORE:**
```
Frontend → http://localhost:8001/api/reports/...
           ❌ Direct request, CORS blocked
```

**AFTER:**
```
Frontend → /api/reports/...
           ↓ (Vite proxy)
           http://localhost:8001/api/reports/...
           ✅ Same-origin request, no CORS
```

### Error Handling

**BEFORE:**
```javascript
try {
  await fetch('http://localhost:8001/api/reports/...')
} catch (error) {
  // ❌ Generic "Network Error"
  // ❌ No details
  // ❌ No recovery
}
```

**AFTER:**
```javascript
try {
  // 1. Test connectivity first
  await reportsApi.ping();
  console.log('✅ Backend is reachable');
  
  // 2. Make actual request
  await reportsApi.listByStudy(studyUID);
  console.log('✅ Found reports');
  
} catch (error) {
  // ✅ Detailed error logging
  console.error('❌ Request failed:', {
    url: fullURL,
    method: 'GET',
    status: error.response?.status,
    message: error.message
  });
  
  // ✅ Graceful fallback
  if (error.message.includes('unreachable')) {
    setIsOfflineMode(true);
    // Show banner with "Test Connection" button
  }
}
```

### Logging Flow

**BEFORE:**
```
[No logs]
❌ Network Error
```

**AFTER:**
```
🌐 Reports API Base URL: /api
🌐 First Request URL: /api/reports/health
🔄 Proxying: GET /api/reports/health → http://localhost:8001/api/reports/health
[REPORTS API] GET /api/reports/health
✅ Response: 200 /api/reports/health
✅ Backend health check passed
📋 ReportingPage Initialized: { studyUID: 'test-123', ... }
🔍 Testing backend connectivity...
✅ Backend is reachable
📋 Looking for existing drafts for study: test-123
```

---

## 🛠️ Component Interaction Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    ReportingPage.tsx                            │
│  - Validates studyUID parameter                                 │
│  - Logs initialization                                          │
│  - Passes props to StructuredReportingUnified                   │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│            StructuredReportingUnified.tsx                       │
│  - Manages workflow state machine                               │
│  - Logs workflow transitions                                    │
│  - Renders appropriate step component                           │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│            UnifiedReportEditor.enhanced.tsx                     │
│  - Calls useReportState hook                                    │
│  - Detects offline mode                                         │
│  - Shows diagnostic banner if needed                            │
│  - Provides "Test Connection" button                            │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                useReportState.ts                                │
│  - Tests backend connectivity (ping)                            │
│  - Loads or creates draft                                       │
│  - Handles errors gracefully                                    │
│  - Creates temp draft if backend down                           │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    ReportsApi.ts                                │
│  - Provides ping() method                                       │
│  - Provides runConnectivityTest() method                        │
│  - Logs all requests/responses                                  │
│  - Uses /api base path (proxy)                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🎯 Request Path Comparison

### Health Check Request

**BEFORE (Failed):**
```
Browser
  ↓ (direct request)
http://localhost:8001/api/reports/health
  ↓
❌ CORS Error
```

**AFTER (Success):**
```
Browser
  ↓ (relative path)
/api/reports/health
  ↓ (Vite proxy on localhost:3010)
http://localhost:8001/api/reports/health
  ↓
✅ Success (200 OK)
```

### Report Creation Request

**BEFORE (Failed):**
```
POST http://localhost:8001/api/reports
  ↓
❌ CORS Error
❌ No auth token sent
❌ Generic error message
```

**AFTER (Success):**
```
POST /api/reports
  ↓ (Vite proxy adds auth header)
POST http://localhost:8001/api/reports
  ↓ (Backend logs request)
[REPORTS API] POST /api/reports
  ↓ (Backend processes)
✅ Success (200 OK)
  ↓ (Response logged)
✅ Response: 200 /api/reports
  ↓ (Frontend receives)
✅ Created draft: SR-2025-001
```

---

## 📊 Error Detection & Recovery

```
┌─────────────────────────────────────────────────────────────────┐
│                    REQUEST INITIATED                            │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
                    ┌────────────────┐
                    │  Ping Backend  │
                    └────────┬───────┘
                             │
                ┌────────────┴────────────┐
                │                         │
                ▼                         ▼
        ┌───────────────┐         ┌──────────────┐
        │   Success     │         │    Failure   │
        │   ✅ 200 OK   │         │  ❌ Timeout  │
        └───────┬───────┘         └──────┬───────┘
                │                         │
                │                         ▼
                │                 ┌──────────────────┐
                │                 │  Set Offline     │
                │                 │  Mode = true     │
                │                 └──────┬───────────┘
                │                         │
                │                         ▼
                │                 ┌──────────────────┐
                │                 │  Show Banner:    │
                │                 │  "API            │
                │                 │  DISCONNECTED"   │
                │                 └──────┬───────────┘
                │                         │
                │                         ▼
                │                 ┌──────────────────┐
                │                 │  Create Temp     │
                │                 │  Local Draft     │
                │                 └──────────────────┘
                │
                ▼
        ┌───────────────┐
        │  Continue      │
        │  Normal Flow   │
        └───────────────┘
```

---

## 🔧 Troubleshooting Decision Tree

```
                    ┌─────────────────────┐
                    │  Network Error?     │
                    └──────────┬──────────┘
                               │
                    ┌──────────┴──────────┐
                    │                     │
                    ▼                     ▼
            ┌───────────────┐     ┌──────────────┐
            │  Check Health │     │  Check       │
            │  Endpoint     │     │  Console     │
            └───────┬───────┘     └──────┬───────┘
                    │                     │
        ┌───────────┴───────────┐         │
        │                       │         │
        ▼                       ▼         ▼
┌───────────────┐       ┌──────────────┐ │
│  200 OK?      │       │  404 Error?  │ │
│  ✅ Backend   │       │  ❌ Route    │ │
│  is running   │       │  not mounted │ │
└───────┬───────┘       └──────┬───────┘ │
        │                       │         │
        ▼                       ▼         ▼
┌───────────────┐       ┌──────────────┐ ┌──────────────┐
│  Check Proxy  │       │  Check       │ │  CORS Error? │
│  Config       │       │  Backend     │ │  ❌ Wrong    │
└───────┬───────┘       │  Routes      │ │  base URL    │
        │               └──────────────┘ └──────────────┘
        ▼
┌───────────────┐
│  Use /api     │
│  not          │
│  localhost:   │
│  8001         │
└───────────────┘
```

---

## 📝 Summary

The fix ensures:
1. ✅ All requests go through Vite proxy (no CORS)
2. ✅ Health endpoint available for testing
3. ✅ Comprehensive logging at every step
4. ✅ Graceful fallback when backend down
5. ✅ Built-in diagnostics for troubleshooting
6. ✅ Clear, actionable error messages

**Result**: No more generic "Network Error" - users and developers get clear, actionable information at every step.
